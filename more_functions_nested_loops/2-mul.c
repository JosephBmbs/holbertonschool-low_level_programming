#include "main.h"

/**
* mul - a function that multiplies two integers.
* @a: the 1st int for the paramaters of mul function
* @b: the 2nd int for the paramaters of mul function
* Return: Always 0.
*/

int mul(int a, int b)
{
return (a * b);
}
