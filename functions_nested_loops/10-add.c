#include "main.h"
#include <stdio.h>

/**
* add - it's a function that adds two integers and returns the result
* @a: first integer
* @b: second integer
* Return: Always 0
*/

int add(int a, int b)
{
	return (a + b);
}
